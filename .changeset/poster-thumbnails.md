---
'@sweet-player/core': minor
---

Add `poster` (cover image shown before playback) and `thumbnails` (WebVTT sprite preview shown on progress-bar hover) options. Both respect `hiddenControls` (`'poster'`, `'thumbnails'`) to skip initialization entirely.
