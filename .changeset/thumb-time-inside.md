---
'@sweet-player/core': patch
---

Fix the thumbnail preview's time label being clipped by the preview box's `overflow: hidden` when placed outside. The time pill is now positioned inside the preview at the bottom-center (matching common video sites).
