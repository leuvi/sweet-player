---
'@sweet-player/core': patch
---

**修复设置面板开关行的点击范围**：此前整行（含标签文字与空白区域）都会触发开关切换，容易误触。现在只有开关控件本身响应点击。开关行与滑块行的 `cursor` 也相应改为 `default`，开关本身保持 `pointer`，鼠标形态即可区分可点区域。

倍速 / 画质 / 画面比例这类导航行不受影响，仍是整行点击进入子菜单。
