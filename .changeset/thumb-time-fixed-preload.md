---
'@sweet-player/core': patch
---

Move the time label to a fixed position above the hover caliper (no longer coupled to the thumbnail preview) and preload sprite images once the VTT track loads, so hovering the progress bar never shows a black tile while the image is still fetching.
